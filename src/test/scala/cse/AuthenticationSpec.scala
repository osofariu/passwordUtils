package cse

import org.scalatest._

class AuthenticationSpec extends path.FunSpec with MustMatchers {
  describe("Usernames/userids") {
    it("must be case insensitive") {
      pending
    }
  }
}
